export default interface privateKeys {
  db: string;
  port: number;
  client_id: string;
  client_secret: string;
  redirect_uri: string;
  consumer_key: string;
  consumer_secret: string;
  access_token: string;
  access_token_secret: string;
  region: string;
  toEmail: string;
  fromEmail: string;
  configurationSet: string;
  recaptchaServer: string;
}
